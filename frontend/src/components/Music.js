import React from 'react'

const Music = (props) => {
    return (
        <div>
            <button className="buttons" onClick={props.func}>Music</button>
        </div>
    )
}

export default Music
